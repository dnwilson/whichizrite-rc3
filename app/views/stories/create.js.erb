//The first line updates the list
$(".ajaxreloadstories").html("<%= escape_javascript(render partial: 'shared/feed') %>");
$('.stories > li:first').effect('highlight', {}, 5000);

// //This next line clears the textarea and textfield
$('textarea').val("");
$('#story_title').val("");
<span class="user-info">
	<span class="avatar">
		<a href="<%= user_path(current_user) %>">
			<%= avatar_for(current_user, "small") %>
		</a>
	</span>
	<span class="user-dets">
		<h2>
			<%= current_user.username %>
		</h2>
		<span>
			<%= link_to "view my profile", current_user %>
		</span><br />
	</span>	
</span>
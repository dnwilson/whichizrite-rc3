<% @user ||= current_user %>
<span class="stats">
	<a href="<%= user_path(@user) %>">
		<strong id="following" class="stat">
			<%= @user.following_users_count %>
		</strong><br />
		following
	</a><a href="<%= user_path(@user) %>">
		<strong id="followers" class="stat">
			<%= @user.followers_count %>
		</strong><br />
		followers
	</a>
</span>